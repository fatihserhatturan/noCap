"use strict";
// Preload runs in a privileged context between the main process and the renderer.
// The app currently needs no custom IPC, so this is intentionally minimal.
// Add contextBridge.exposeInMainWorld() calls here if native features are needed.
